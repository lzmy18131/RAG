# 依赖的单一来源是 pyproject.toml（[project].dependencies）。
# 本文件保留为 legacy install 路径（pip install -r requirements.txt），
# 与 pyproject 保持同步；新增/变更依赖请同时修改两处。
#
# 开发与评测依赖：pip install -e ".[dev,eval]"

# Configuration
pydantic-settings>=2.0
python-dotenv>=0.21
pyyaml>=6.0

# Web 框架
fastapi>=0.115
uvicorn[standard]>=0.34
python-multipart>=0.0.9

# Deep learning（本地模型；Demo Mode 不需要）
torch>=2.0
sentence-transformers>=2.7

# Vector database（Milvus Lite 单进程模式）
pymilvus>=2.4
milvus-lite>=3.0

# OpenAI-compatible API client（LLM / VLM）
openai>=1.0

# PDF parsing
pymupdf>=1.28

# 检索
jieba>=0.42
rank-bm25>=0.2

# 编排
langgraph>=1.0
langchain-openai>=0.3

# RAG 评测（仅离线评测需要）
ragas==0.4.3
datasets>=2.0

# Dev 依赖（pip install -e ".[dev]"）
# pytest>=8.0
# pytest-asyncio>=0.24
# pytest-cov>=5.0
# ruff>=0.8
# mypy>=1.10
